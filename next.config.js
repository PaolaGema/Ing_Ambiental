/** @type {import('next').NextConfig} */
// next.config.js
const nextConfig = {
    images: {
      domains: ["serviciopagina.upea.bo"], // Agrega el dominio aquí
    },
  };
  
  module.exports = nextConfig;

  